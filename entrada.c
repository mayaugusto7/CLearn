#include <stdio.h>
#include <stdlib.h>

int main() {
    char letra;
    int num;
    float flo;
    double dec;

    scanf("%c", &letra);
    scanf("%d", &num);
    scanf("%f", &flo);
    scanf("%lf", &dec);

    system("pause");

    return 0;
}